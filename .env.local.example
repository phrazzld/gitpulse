# GitPulse Environment Variables
# 
# SECURITY WARNING: NEVER commit real credentials to version control!
# Copy this file to .env.local and fill in your actual values
#
# This file (.env.local.example) should contain only placeholders
# The actual .env.local file is already in .gitignore

# NextAuth Configuration
# Generate a secure secret WITHOUT special characters:
# openssl rand -base64 32 | tr -d "=+/" | cut -c1-32
NEXTAUTH_SECRET=your_nextauth_secret_here_32_chars_min

# GitHub OAuth Application
# Create at: https://github.com/settings/applications/new
# Callback URL: http://localhost:3000/api/auth/callback/github (dev)
# Callback URL: https://your-domain.com/api/auth/callback/github (prod)
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret

# Google Gemini API
# Get your API key from: https://makersuite.google.com/app/apikey
# Restrict key to specific APIs and domains for security
GEMINI_API_KEY=your_gemini_api_key_here

# NextAuth URL (optional for local dev, Vercel sets automatically)
# NEXTAUTH_URL=http://localhost:3000